# ── Secrets ──
.env
config.yaml
*.pem
*.key

# ── Python ──
__pycache__/
*.pyc
*.pyo
venv/
.env/
*.egg-info/
dist/*
!dist/arch
!dist/arch/*
!dist/arch/PKGBUILD
!dist/arch/.SRCINFO
build/
*.so

# ── Logs ──
logs/
*.log

# ── Models (downloaded separately) ──
models/
!models/.gitkeep

# ── OS ──
.DS_Store
*.swp
*.swo
*~
Thumbs.db

# ── Backups ──
*.bak
*.orig

# ── IDE ──
.vscode/
.idea/
*.sublime-*

# ── Temp ──
/tmp/jarvis

# ── Tests ──
.pytest_cache/
.coverage
coverage/
htmlcov/
*.coverage

# ── Session state (contains secrets) ──
SESSION.md

# ── Local artifacts (review) ──
*.joblib
audit-review/

# ── jarvis-ui ──
node_modules/
src-tauri/target/
src-tauri/binaries/
jarvis-ui/src-tauri/binaries/
jarvis-ui/dist/*
!jarvis-ui/dist/arch
!jarvis-ui/dist/arch/*
!jarvis-ui/dist/arch/PKGBUILD
!jarvis-ui/dist/arch/.SRCINFO
HANDOFF.md

# ── local tooling ──
opencode.jsonc

# ── Arch packaging (dist/arch) — binaries ignored, PKGBUILD/.SRCINFO kept (see Python section) ──
dist/arch/*.pkg.tar.*
dist/arch/*.pkg.tar.zst
dist/arch/*.deb
dist/arch/src/
dist/arch/pkg/

# личный промпт владельца — в репо не нужен
docs/site/V0_PROMPT.md
