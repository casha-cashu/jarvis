# Pinned to known-good versions: verified in repo venv (1260 pytest green).
# Чистая установка ставит ТОЛЬКО это, а не newest с PyPI. Бампить осознанно,
# после прогона полного гейта. Исключение — torch (см. ниже).
pyyaml==6.0.3
pyaudio==0.2.14
numpy==2.4.6
# torch НЕ пиним: у разработчиков CUDA-сборка, в docker/CI ставится CPU
# (torch --index-url .../whl/cpu, см. docker/Dockerfile.*). Точный == притянул
# бы CUDA-бандл (~3-5GB) на CPU-машины. Диапазон — осознанно.
torch>=2.0
faster-whisper==1.2.1
silero-vad==6.2.1
anthropic==0.104.1
openai==2.46.0
requests==2.34.2
gtts==2.5.4
scikit-learn==1.9.0
joblib==1.5.3
pydantic==2.13.4
filelock==3.29.0
ruamel.yaml==0.19.1
rapidfuzz==3.14.5
audioop-lts==0.2.2; python_version >= '3.13'

# ── Опционально ──
# vosk: wheels только для Python 3.10-3.12; на 3.13+ используйте
# stt.engine: whisper или ставьте vosk вручную (alphacephei.com/vosk)
# vosk==0.3.45  (extras: pip install -e ".[vosk]")
# aiogram: только для Telegram-бота (pip install -e ".[telegram]")
# aiogram==3.31.0
